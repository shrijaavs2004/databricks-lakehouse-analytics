## Modeling

This module contains notebooks used for feature engineering and baseline machine learning models.

The focus is on translating curated analytical data into ML-ready datasets and evaluating baseline model performance rather than extensive model tuning.

### Responsibilities
- Perform feature engineering from Gold tables
- Train baseline predictive models
- Evaluate model performance using standard metrics
- Interpret results for downstream decision-making
